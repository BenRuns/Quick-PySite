import yaml
import json


class Website:
	"""This is the main object for """
	def __init__(self, arg ):
		print arg 
		self.data = arg

		#goes through the data and loads it 
	def load_db(self):
	# """ this goes through the data folder uses the data manifest to create 
	# a dictionary of models """
		pass

	def build_site(self):
		""" builds the site to public according to the manifest """
		pass

	def add_model_to_db(self, model):

		self.data 

		